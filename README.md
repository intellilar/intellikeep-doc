# IntelliKeep Documentation

Documentation site for the [IntelliKeep](https://github.com/intellilar/intellikeep) Home Assistant integration, built with [Docusaurus 3](https://docusaurus.io).

Published at: **https://docs.intellikeep.intellilar.com**

## Local development

```bash
npm install
npm start        # dev server at localhost:3000 with hot reload
```

## Build

```bash
npm run build    # production build into build/
npm run serve    # serve the production build locally
```

## Other commands

```bash
npm run typecheck   # TypeScript type check (no emit)
npm run clear       # clear Docusaurus cache (.docusaurus/ and build/)
```
